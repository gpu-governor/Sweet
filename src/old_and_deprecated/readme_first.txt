this folder "old and depriecated" contains files that while previous used in the project but has been replaced by better method, they are stored here for documenting.

one example is the widget.h was the file that were #included in users program, but now we moved everything to sweet.h so sweet can be included.

other files contain old implementation like the old event system or the immediate mode implementation
